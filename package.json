{
  "name": "assessment-task-aws",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "package": "npm run build && powershell Compress-Archive -Path dist/*, node_modules -DestinationPath deployment-package.zip -Force"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.637.0",
    "@aws-sdk/lib-dynamodb": "^3.637.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "typescript": "^5.6.3"
  }
}